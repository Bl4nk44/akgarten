# Backend ENV (production) – fill safely via secrets or CI/CD, do NOT commit real values
# Copy this as a reference; do not store secrets in git.

# Email (Resend)
RESEND_API_KEY=__SET_IN_SECRET_STORE__
TARGET_EMAIL=kontakt@akgarten.com

# OpenAI
OPENAI_API_KEY=__SET_IN_SECRET_STORE__
# Recommended models
OPENAI_MODEL=gpt-5
OPENAI_FALLBACK_MODEL=gpt-4.1

# CORS
# Comma-separated list of allowed origins
ALLOWED_ORIGIN=https://akgarten.com,https://www.akgarten.com

# App metadata (exposed in /api/health)
APP_VERSION=vYYYY-MM-DD

# Docker/Deploy notes
# - Provide these via docker-compose.yml or your secret manager (env file should not be committed with real values)
# - Ensure reverse proxy routes `/api/` to backend and sets client_max_body_size 10m, proxy_read_timeout 60s
